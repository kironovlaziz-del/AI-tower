# backend
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.env

# frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local

# misc
.DS_Store
ai-control-tower.zip
all_code.txt
*.zip
*.log
backend.log
frontend.log
celery.log
.env
.env.*
.en
!.env.example

# Trained model artifacts and uploaded datasets
backend/data/

# NER training data & model binaries (rebuilt locally)
backend/ner_training/data/
backend/ner_training/output/

# NER training data & model binaries (rebuilt locally)
backend/ner_training/data/
backend/ner_training/output/


# agent: secrets and compiled binary (source is committed, these are not)
agent/config.json
agent/shadow-agent

# temporary patch scripts and backups (dev artifacts)
frontend/*_patch.py
frontend/**/*.bak
*.bak

# Celery beat scheduler state (runtime, per-server)
backend/celerybeat-schedule
backend/celerybeat-schedule.db
