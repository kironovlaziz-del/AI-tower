# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Config & validation
pydantic==2.10.4
pydantic-settings==2.7.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# File uploads
python-multipart==0.0.20
email-validator==2.2.0

# System info
psutil==6.1.1

# Task queue
celery[redis]==5.4.0
redis==5.2.1

# Database (sync, for Celery workers)
psycopg2-binary==2.9.10

# Classic ML
pandas==2.2.3
scikit-learn==1.6.0
joblib==1.4.2
numpy==2.2.1

# Crypto
cryptography==44.0.0

# HTTP client
httpx==0.28.1

# ---------------------------------------------------------------------------
# Optional: transformer fine-tuning (heavy, ~2 GB of wheels).
# Not installed by default — the UI shows the transformer task types and the
# compute detector reports GPU/VRAM, but training_tasks.py imports these
# lazily. Install on a GPU box (or a large CPU box for tiny models) with:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-ml.txt
#
# See requirements-ml.txt for the pinned transformer stack.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Development / testing
# ---------------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1
docker==7.1.0
spacy==3.8.2
